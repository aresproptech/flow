CREATE TABLE "public"."opportunity_activities" (
  "id"               bigint                   GENERATED BY DEFAULT AS IDENTITY (SEQUENCE NAME "public"."order_contacts_id_seq") NOT NULL,
  "created_at"       timestamp with time zone NOT NULL DEFAULT now(),
  "opportunity_id"   bigint,
  "fecha"            date,
  "contact_id"       bigint,
  "memo"             character varying,
  "resultado"        boolean,
  "domicilio"        character varying,
  "event_type"       text                     NOT NULL DEFAULT 'legacy'::text,
  "actor_profile_id" bigint,
  "effective_at"     timestamp with time zone,
  "metadata"         jsonb                    NOT NULL DEFAULT '{}'::jsonb,
  "parent_event_id"  bigint,
  "updated_at"       timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT "oportunity_contacts_oportunity_id_fkey" FOREIGN KEY (opportunity_id) REFERENCES public.opportunities(id),
  CONSTRAINT "opportunity_contacts_event_type_check"
    CHECK
    ((event_type = ANY (ARRAY['legacy'::text, 'activity'::text, 'note'::text, 'call'::text, 'contact'::text, 'valuation'::text, 'valuation_updated'::text, 'rg'::text, 'rg_updated'::text,
    'lead_created'::text,
    'lead_imported'::text,
    'lead_updated'::text, 'lead_deleted'::text, 'phase_changed'::text, 'visit_created'::text, 'visit_updated'::text, 'order_created'::text, 'order_updated'::text,
    'document_uploaded'::text, 'document_viewed'::text]))),
  CONSTRAINT "order_contacts_pkey" PRIMARY KEY (id),
  CONSTRAINT "opportunity_contacts_parent_event_id_fkey" FOREIGN KEY (parent_event_id) REFERENCES public.opportunity_activities(id) ON DELETE SET NULL,
  CONSTRAINT "opportunity_contacts_actor_profile_id_fkey" FOREIGN KEY (actor_profile_id) REFERENCES public.profiles(id) ON DELETE SET NULL
);

ALTER TABLE "public"."opportunity_activities"
  ENABLE ROW LEVEL SECURITY;

CREATE INDEX idx_opportunity_contacts_actor_profile_id ON public.opportunity_activities USING btree (actor_profile_id)
  WHERE (actor_profile_id IS NOT NULL);

CREATE INDEX idx_opportunity_contacts_event_type_fecha ON public.opportunity_activities USING btree (event_type, fecha DESC);

CREATE INDEX idx_opportunity_contacts_opportunity_event_created ON public.opportunity_activities USING btree (opportunity_id, event_type, created_at DESC);

CREATE POLICY "contacts_insert_by_opportunity" ON "public"."opportunity_activities"
  FOR INSERT
  TO "authenticated"
  WITH CHECK (public.crm_can_write_opportunity(opportunity_id));

CREATE POLICY "contacts_select_by_opportunity" ON "public"."opportunity_activities"
  FOR SELECT
  TO "authenticated"
  USING (public.crm_can_read_opportunity(opportunity_id));

CREATE POLICY "activities_update_functional_records" ON "public"."opportunity_activities"
  FOR UPDATE
  TO "authenticated"
  USING ((public.crm_can_write_opportunity(opportunity_id) AND (event_type = ANY (ARRAY['contact'::text, 'rg'::text, 'valuation'::text]))))
  WITH CHECK ((public.crm_can_write_opportunity(opportunity_id) AND (event_type = ANY (ARRAY['contact'::text, 'rg'::text, 'valuation'::text]))));

GRANT INSERT, SELECT, UPDATE ON TABLE "public"."opportunity_activities" TO "appsheet_user";

GRANT DELETE, INSERT, MAINTAIN, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE ON TABLE "public"."opportunity_activities" TO "postgres", "service_role";

COMMENT ON TABLE "public"."opportunity_activities" IS 'Oportunidad Contactos';

REVOKE ALL ON TABLE "public"."opportunity_activities" FROM "authenticated";

GRANT INSERT, SELECT, UPDATE ON TABLE "public"."opportunity_activities" TO "authenticated";
